import { GraphPageClient } from "@/components/graph/GraphPageClient";

/** Skip route-level loading skeleton — graph renders instantly from sample data */
export default function GraphPage() {
  return <GraphPageClient />;
}
